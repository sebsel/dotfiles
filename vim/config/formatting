"==> Formatting

"==> The tabs or spaces debate

"-> keep tabstop equal to softtabstop and shiftwidth
set tabstop=2 softtabstop=2 shiftwidth=2

nmap <leader>2 :set tabstop=2 softtabstop=2 shiftwidth=2<cr>
nmap <leader>4 :set tabstop=4 softtabstop=4 shiftwidth=4<cr>

"-> use spaces instead of tabs
"   <c-v><c-i> in insert mode inserts a tab character if you need it
set expandtab
set smarttab

" vim: ft=vim
