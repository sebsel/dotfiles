# === Laravel ===

alias art="php artisan"
alias mfs="php artisan migrate:fresh --seed"
alias queue="php artisan queue:work"
alias logs="tail -f -n200 storage/logs/*.log"
alias wlogs="tail -f -n200 web/wp-content/debug.log"
